# Frontend Environment Variables

# API endpoint (required for production)
# In development, leave empty to use Vite proxy (/api -> localhost:5000)
VITE_API_URL=

# Socket.IO endpoint (optional)
# Defaults to API URL without /api suffix
# Example for production: https://api.yourdomain.com
VITE_SOCKET_URL=
